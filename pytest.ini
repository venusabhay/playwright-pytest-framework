[pytest]
addopts = -ra --strict-markers -p no:pytest_playwright --html=reports/pytest_report.html --self-contained-html
testpaths = tests
markers =
    smoke: quick smoke tests
    ui: browser UI tests
    regression: regression coverage
    api: API-oriented tests
